# examples

Artifact examples validated by the gate (`node tools/validate.mjs --all`).
Subjects are fictional or composite so the repository's own examples honor the
honest frame (spec 5, GH-01); illustrative titles are marked as such. An example
that names a living person does not belong here.
