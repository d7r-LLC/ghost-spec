# schema

Draft JSON Schemas for the artifacts GHOST names, one directory per major spec
version. Each schema's description cites the sections it interprets. Draft means:
these interpret a draft spec, the author has not ratified them, and a spec change
that invalidates one is caught by the conformance register diff, not by the schema
itself. The site renders a reference page from these at /schema/.
